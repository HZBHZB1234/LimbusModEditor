<UserControl x:Class="LimbusModEditor.App.PresetWorkbenchPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:LimbusModEditor.App">
    <!--

        plan-11：人格预设卡片流页面（三列骨架由共享 WorkbenchShell 提供）。

        版面：浏览列（搜索行 / 下滑卡片流：每张卡是一个人格式小人的立绘 + 名字）
              — 分隔条 — 详情列（点卡片后展开这个人格的数据 / 文本 / 音频 / 图像 …）。

        ★ 本文件刻意只有骨架这一个子元素：浏览列与详情列的控件都在代码里构建，
          通过 Shell.SetBrowseContent / Shell.SetEditContent 放进骨架的 ContentControl。
          若同一块内容同时写在 XAML 里，同一个 Visual 会有两个父级，布局时抛
          「在指定子级连接到新的父 Visual 之前必须断开与当前父 Visual 的连接」并崩进程
          （与 TextWorkbenchPage.xaml 的注释同因）。

        数据：cache/relation-index.db（启动扫描第 6 步由四个索引库派生），
        只读反查；卡片封面按 Sprite/Unit/Profile → UnitCgThumbnail → Unit/CG … 的优先级挑。

        设计令牌与控件样式一律走 Themes/WorkbenchStyles.xaml（本文件与代码后置不出现色值字面量）。
    -->
    <local:WorkbenchShell x:Name="Shell"/>
</UserControl>
